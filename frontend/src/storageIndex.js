import React from "react";
import ReactDOM from "react-dom/client";
import Storage from "./Storage";
import "./App.css";

ReactDOM.createRoot(document.getElementById("storage-root")).render(
  <Storage />
);
